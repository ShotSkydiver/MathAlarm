<phone:PhoneApplicationPage
    x:Class="MathClock.AboutPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait" Orientation="Portrait"
    mc:Ignorable="d"
    shell:SystemTray.IsVisible="True"
    shell:SystemTray.BackgroundColor="#FF1BA0E1"
    shell:SystemTray.ForegroundColor="White">

    <phone:PhoneApplicationPage.ApplicationBar>
        <shell:ApplicationBar IsVisible="True" IsMenuEnabled="True">
            <shell:ApplicationBarIconButton x:Name="okButton"
                                            IconUri="/Toolkit.Content/ApplicationBar.Check.png"
                                            Text="ok"
                                            Click="OKButton_Click"/>
        </shell:ApplicationBar>
    </phone:PhoneApplicationPage.ApplicationBar>

    <!--LayoutRoot is the root grid where all page content is placed-->
    <Grid x:Name="LayoutRoot" Background="#FF1BA0E1">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!--TitlePanel contains the name of the application and page title-->
        <Image HorizontalAlignment="Left" Source="Images/SplashScreenImage.screen-test1.jpg" Stretch="None" Height="805" Margin="-147,-569,-69,0" VerticalAlignment="Top" Width="696" Grid.RowSpan="2"/>

        <!--TitlePanel contains the name of the application and page title-->
        <StackPanel Grid.Row="0" Margin="12,17,0,28">

            <!--<TextBlock Text="about this app" Margin="-4,-7,13,0" Style="{StaticResource PhoneTextTitle1Style}" d:IsHidden="True"/>-->
        </StackPanel>

        <!--ContentPanel - place additional content here-->
        <Grid x:Name="ContentPanel" Grid.Row="1" Margin="12,0,12,0">
        	<TextBlock HorizontalAlignment="Left" TextWrapping="Wrap" VerticalAlignment="Top" Height="423" Width="399" Margin="30,203,0,0">
        		<Run Text="MathAlarm is an app that functions like a simple alarm clock, but upon reaching the scheduled alarm time, it requires the user to solve a math problem first before being able to disable the alarm."/>
        		<LineBreak/>
        		<Run/>
        		<LineBreak/>
        		<Run Text="MathAlarm is based partially off of the MSDN SDK sample alarm clock with voice commands."/>
        		<LineBreak/>
        		<Run Text=""/>
        		<LineBreak/>
        		<Run Text="I hope you like this app or find it useful in some way, it's the first app I've developed for Windows Phone. "/>
        		<LineBreak/>
        		<Run/>
        		<LineBreak/>
        		<Run Text="For feedback and bugs, "/>
        	</TextBlock>
        </Grid>
        <Button x:Name="emailButton" Content="email me." HorizontalAlignment="Left" VerticalAlignment="Top" Margin="239,559,0,0" Grid.Row="1" FontSize="20" FontFamily="Segoe UI" BorderThickness="1" Padding="3" Click="emailButton_Click"/>
        <Rectangle Fill="#FF1BA0E1" HorizontalAlignment="Left" Height="55" Stroke="Black" VerticalAlignment="Top" Width="276" Margin="97,73,0,0" Grid.Row="1" StrokeThickness="0"/>
        <TextBlock HorizontalAlignment="Left" TextWrapping="Wrap" Text="MathAlarm" VerticalAlignment="Top" FontSize="32" FontFamily="Segoe WP Semibold" Margin="155,108,0,0" Grid.Row="1"/>
    </Grid>

</phone:PhoneApplicationPage>