<phone:PhoneApplicationPage
    x:Class="MathClock.AlarmRinging"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait" Orientation="Portrait"
    mc:Ignorable="d"
    shell:SystemTray.IsVisible="True">

    <phone:PhoneApplicationPage.ApplicationBar>
        <shell:ApplicationBar IsVisible="True" IsMenuEnabled="True">
            <shell:ApplicationBarIconButton x:Name="solveAlarm"
                                            IconUri="/Toolkit.Content/ApplicationBar.Check.png"
                                            Text="solve"
                                            Click="Solve_Click"/>
            
        </shell:ApplicationBar>
    </phone:PhoneApplicationPage.ApplicationBar>

    <!--LayoutRoot is the root grid where all page content is placed-->
    <Grid x:Name="LayoutRoot" Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!--TitlePanel contains the name of the application and page title-->
        <StackPanel Grid.Row="0" Margin="12,17,0,28">
            <TextBlock Text="alarm ringing" Margin="9,-7,0,0" Style="{StaticResource PhoneTextTitle1Style}"/>
        </StackPanel>

        <!--ContentPanel - place additional content here-->
        <Grid x:Name="ContentPanel" Grid.Row="1" Margin="12,0,12,0">
        	<TextBlock x:Name="equation" HorizontalAlignment="Left" TextWrapping="Wrap" Text="What is 44 + 15?" VerticalAlignment="Top" FontFamily="Segoe WP Light" Height="60" Width="409" Margin="5,1,0,0" FontSize="32" FontStyle="Italic"/>
        	<TextBox x:Name="answerInput" HorizontalAlignment="Left" Height="72" Margin="0,36,0,0" TextWrapping="Wrap" Text=" " VerticalAlignment="Top" Width="238" FontFamily="Portable User Interface" InputScope="Number" KeyDown="KeyDownHandler"/>
        	<!--<TextBlock x:Name="displayAnswer" HorizontalAlignment="Left" Margin="309,75,0,0" TextWrapping="Wrap" Text="Answer: " VerticalAlignment="Top"  FontFamily="Portable User Interface"/>-->

        </Grid>
    </Grid>

</phone:PhoneApplicationPage>